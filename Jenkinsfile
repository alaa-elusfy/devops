pipeline {
    agent any
    stages {
      stage("INIT") {
        steps {
            echo "INIT"
            sh "ls -la"
        }
      }
      stage("PACKAGE") {
        steps {
            echo "PACKAGE"
        }
      }
    }
}
